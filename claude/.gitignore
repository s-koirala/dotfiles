# =====================================================================
# Sensitive files — MUST NEVER COMMIT
# =====================================================================
.credentials.json
mcp-needs-auth-cache.json
settings.local.json
*.token
*.secret
*.pem
*.key
.env
.env.*

# =====================================================================
# Runtime state — ephemeral, regenerated by Claude Code
# =====================================================================
.claude/
.last-cleanup
backups/
cache/
debug/
file-history/
ide/
plugins/
projects/
scheduled_tasks.lock
session-env/
sessions/
shell-snapshots/
stats-cache.json
statsig/
telemetry/
todos/

# =====================================================================
# Per-user state (not for shared dotfiles)
# =====================================================================
memory/
config.toml

# =====================================================================
# Python build artifacts (in case scripts/ accumulates compiled files)
# =====================================================================
__pycache__/
*.py[cod]
*$py.class
*.egg-info/
.pytest_cache/
.ruff_cache/
.mypy_cache/

# =====================================================================
# OS / editor cruft
# =====================================================================
.DS_Store
Thumbs.db
*.swp
*.swo
*~

# =====================================================================
# Allow-list — these match patterns above but ARE tracked
# =====================================================================
!mcp.json
