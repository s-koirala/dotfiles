# Bootstrap directive — consolidated

The verbose multi-stage bootstrap directive that previously lived here has
been consolidated into [INSTALL.md](INSTALL.md).

For the AI-paste workflow, see [INSTALL.md §Quickstart — Claude Code](INSTALL.md#quickstart--claude-code-ai-assisted)
(the collapsible `<details>` block).

For the manual workflow, see [INSTALL.md §Quickstart — manual](INSTALL.md#quickstart--manual-no-ai).

This stub is kept so existing references (e.g., session-history transcripts
that mention `Bootstrap_Directive.md`) still resolve.
